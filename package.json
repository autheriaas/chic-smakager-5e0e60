{
  "name": "autheria-site",
  "version": "1.0.0",
  "private": true,
  "description": "Claudia's Community static site and Netlify Functions",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "netlify dev --offline",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test"
  },
  "engines": {
    "node": ">=24.21.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "netlify-cli": "27.8.0"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
